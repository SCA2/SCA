<% provide(:title, "Cart " + @cart.id.to_s) %>

<div class="box_2">
	<div class="row">
		<div class="small-12 columns" style="margin: 1em 1em 0em 0em">
			<%= render 'shared/errors', object: @cart %>
			<%= render 'cart', cart: @cart %>
		</div>
	</div>	
</div>

