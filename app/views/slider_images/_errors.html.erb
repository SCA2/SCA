<% if @slider_image.errors.any? %>
	<div id="error_explanation">
		<h6><%= pluralize(@slider_image.errors.count, "error") %> 
			kept this FAQ from being saved: </h6>
		<ul>
			<% @slider_image.errors.full_messages.each do |msg| %>
			<li><%= msg %></li>
			<% end %>
		</ul>
	</div>
<% end %>
