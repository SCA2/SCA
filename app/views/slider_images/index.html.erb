<% provide(:title, 'Slider Images') %>
<div class="box_2">
	<div class="row">
		<div class="small-12 columns">
			<%= render partial: 'product', collection: @slider_images %>
			<% if signed_in_admin? %>
				<div id= "index-actions">
					<%= link_to 'New Image', new_slider_image_path, label: 'New Image', class: "admin-button" %>
				</div>
			<% end %>
		</div>
	</div>	
</div>
