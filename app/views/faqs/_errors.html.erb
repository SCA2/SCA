<% if @faq.errors.any? %>
	<div id="error_explanation">
		<h6><%= pluralize(@faq.errors.count, "error") %> 
			kept this FAQ from being saved: </h6>
		<ul>
			<% @faq.errors.full_messages.each do |msg| %>
			<li><%= msg %></li>
			<% end %>
		</ul>
	</div>
<% end %>
