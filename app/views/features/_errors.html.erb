<% if @feature.errors.any? %>
	<div id="error_explanation">
		<h6><%= pluralize(@feature.errors.count, "error") %> 
			kept this feature from being saved: </h6>
		<ul>
			<% @feature.errors.full_messages.each do |msg| %>
			<li><%= msg %></li>
			<% end %>
		</ul>
	</div>
<% end %>
