%tr
  %td= link_to category.name, edit_product_category_path(category)
  %td= category.sort_order
  %td= link_to "edit", edit_product_category_path(category), method: :get
  %td= link_to "delete", category, method: :delete, data: { confirm: "Are you sure?" }